[tool:pytest]
# --doctest-modules runs the examples embedded in ductus/ docstrings as tests.
# testpaths keeps collection off setup.py and versioneer.py at the repo root.
addopts = --doctest-modules
testpaths =
    tests
    ductus
